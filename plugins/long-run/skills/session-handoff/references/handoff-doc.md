# 인계 문서 템플릿

받는 세션은 이 문서 **하나**로 출발한다. 이전 대화는 볼 수 없다 — 여기 없는 것은 없는 것이다.

**본문 합성은 `handoff` 스킬이 소유한다.** 대화 압축, 다른 산출물(spec·ADR·이슈·커밋·diff)을 베끼지 않고
경로로 가리키기, 비밀 정보 삭제, 다음 세션이 부를 스킬 지목 — 그 규칙은 거기 있고 여기서 되풀이하지
않는다. 이 문서가 더하는 것은 **그 본문에 씌우는 형식 계약**이다.

계약 넷:

0. **사용자 원칙을 §0 에 싣는다 — 최우선이고, 인계마다 그대로 이어진다.** 사용자가 이 작업에 준 운영 원칙
   (멈춤 조건·분할 규칙·목적지·팬아웃 한도 등)은 요약하지 말고 문장째로 옮긴다. 이전 인계 문서에 §0 이
   있으면 **거기서 그대로 가져와 이번 세션에서 추가된 것만 덧붙인다.** 이 절이 빠지면 새 세션은 규약을
   모른 채 기본값으로 굴러가고, 사용자가 같은 말을 다시 해야 한다.
1. **확인한 것과 추정한 것을 섞지 않는다.** 각 사실 옆에 근거(파일 경로·명령·URL)를 붙인다. 근거를 못
   대면 `미확인` 으로 적는다. 받는 세션이 추정을 사실로 믿고 그 위에 쌓는 것이 이 인계의 가장 큰 실패다.
2. **안 한 일은 안 했다고 적는다.** 왜 못 했는지 해명하지 않는다 — 상태만 적는다.
3. **경로는 전부 절대경로.** 받는 세션의 cwd 는 다를 수 있다.

아래를 그대로 채운다. 해당 없는 절은 지우지 말고 `해당 없음` 이라고 적는다 — 빈칸과 "없음"은 다르다.

🚨 `- **worktree**:` 줄은 **형식을 지킨다.** `handoff.mjs send` 가 이 줄을 읽어 대상 탭과 대조하고,
어긋나면 보내기를 막는다. 줄이 없으면 대조 없이 통과한다.

## 쓴 뒤 — 문서만으로 재개 가능한지 검증한다

보내기 전에 쓴 문서를 **다시 읽고** 자문한다. 대화 기억을 지운 채 이 문서만 받았다면,

- 다음에 뭘 해야 하는지, 어느 스킬을 불러야 하는지 알 수 있는가
- 확정된 값과 아직 추정인 값을 구분할 수 있는가
- 이미 실패한 접근을 다시 시도하지 않겠는가
- 열려 있는 자원(dev 서버·포트·브라우저 세션)을 중복 기동하지 않겠는가

하나라도 아니면 문서를 고친다. 이 검증을 건너뛴 인계는 새 세션이 처음부터 다시 파는 것으로 끝난다.

---

```markdown
# 인계 NN — {한 줄 과업}

- **일시**: {YYYY-MM-DD HH:mm}
- **인계 사유**: {컨텍스트 포화 / 단계 경계 / 그 외}
- **worktree**: {절대경로} (브랜치 `{브랜치}`)
- **bd 정본**: `{부모id}` — 진행 상태는 이 문서가 아니라 `bd children {부모id}` 가 정본이다
- **이전 인계**: {handoff-NN.md 경로 또는 없음}

## 0. 사용자 원칙 — 최우선, 다음 인계에도 그대로 옮긴다

{사용자가 준 운영 원칙을 문장째로. 이전 인계의 §0 + 이번 세션에서 추가된 것.
아래 모든 절보다 우선하며, 충돌하면 여기가 이긴다고 명시한다.}

## 1. 지금 하는 일

{무엇을 왜 하는 중인지 3~5줄. 티켓 번호와 증상이 아니라 '지금 세션이 뭘 하던 중인지'.}

## 2. 확정된 사실

| 사실 | 근거 |
|---|---|
| {예: 기대 색은 #9B9B9B} | {절대경로 또는 URL, 문서 페이지} |

**미확인으로 남은 것**: {있으면 나열, 없으면 '없음'}

## 3. 한 일

- {변경한 파일과 그 이유. 커밋했으면 커밋 해시, 안 했으면 '워킹트리에만 있음'}
- {돌린 검증과 결과. 통과/실패를 숫자로 — '테스트 통과' 가 아니라 '2447/108, 기준선과 동일'}

## 4. 다음 작업

1. **{첫 작업}** — 호출할 스킬: `{plugin:skill}` / 완료조건: {무엇이 되면 끝인가}
2. {그다음}

첫 작업은 **스킬 호출부터** 시작한다. 이 문서의 요약으로 대신하지 않는다 — 문서만 따라가면 스킬이
소유한 규약이 통째로 빠진다(선례: 앞 단계들을 건너뛴 채 수정부터 들어간 세션).

## 5. 열린 자원

| 자원 | 상태 |
|---|---|
| dev 서버 | {포트 / 안 띄움 — 새로 띄우지 말고 먼저 확인할 것} |
| 브라우저 세션 | {세션명 / 없음} |
| 그 외 | {녹화 중, 임시 파일, 백그라운드 작업 등} |

## 6. 함정 / 이미 실패한 접근

- {같은 벽에 다시 부딪히지 않도록. 시도했다 안 된 것과 그 증상.}

## 7. 완료 통보

{이 세션이 상위 코디네이터에게 위임받은 워커였으면 통보 명령 전문을 그대로 적는다.
아니면 '없음 — 완료는 사용자에게 직접 보고한다'.}
```
