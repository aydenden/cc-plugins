{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Codex review verdict",
  "description": "JSON schema for codex review output in review-loop refinement cycle.",
  "type": "object",
  "additionalProperties": false,
  "required": ["status"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["complete", "incomplete", "failed"],
      "description": "complete = ready to ship; incomplete = needs refinement; failed = abort loop"
    },
    "issues": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Concrete problems found in the implementation. Empty when status=complete."
    },
    "next_action": {
      "type": "string",
      "description": "One-line directive for the opencode crew on the next iteration. Required when status=incomplete."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Reviewer confidence in the verdict (0-1)."
    }
  }
}
